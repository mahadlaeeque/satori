# =============================================================================
# Transitional dependency manifest.
# =============================================================================
# Source of truth is pyproject.toml + uv.lock. This file is kept in sync by
# hand for now so any tooling that doesn't speak pyproject.toml (some
# scratch scripts, CI snippets) still resolves. Remove once everything is on
# uv.
# =============================================================================

# Web framework
fastapi==0.115.0
uvicorn[standard]>=0.30.0
gunicorn>=21.2.0
python-multipart>=0.0.9
jinja2>=3.1.4
pydantic>=2.7.0

# Google Cloud
google-genai>=1.0.0
google-cloud-bigquery==3.25.0
google-cloud-secret-manager>=2.20.0
google-cloud-firestore>=2.16.0

# Data
pandas==2.2.0
db-dtypes==1.3.0

# HTTP
requests>=2.31.0

# Observability — OpenTelemetry + Cloud Trace exporter. Tracing is gated at
# runtime on the K_SERVICE env var (set by Cloud Run) so these deps stay
# inert in local dev.
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-instrumentation-fastapi>=0.48b0
opentelemetry-instrumentation-requests>=0.48b0
opentelemetry-exporter-gcp-trace>=1.7.0
