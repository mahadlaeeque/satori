# Keep the container image lean and free of secrets.
# Anything matched here is excluded from the build context.

# Python cruft
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
.pytest_cache/
.mypy_cache/
.coverage
.tox/

# Local virtualenvs (we build a fresh venv inside the image)
venv/
.venv/
env/

# Frontend build artefacts — the Dockerfile's frontend-builder stage runs
# `npm ci && npm run build` inside the container, so we don't ship the
# user's local node_modules or stale dist/ output. (The fresh dist/ produced
# inside the container IS shipped — that COPY happens after this exclusion.)
frontend/node_modules/
frontend/dist/
frontend/.vite/
**/node_modules/

# Local state — must NEVER ship in the image
chat_history.json
settings.json

# Local raw data (lives in Cloud Storage in production)
data/

# Local secrets
service-account/
*.pem
*.key
*.crt
.env
.env.*

# Editor / OS / IDE
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Git
.git/
.gitignore
.gitattributes

# Top-level docs and assets that aren't served by the app
# (note: anything inside templates/ and static/ IS still included)
/Platform 7 Data Requirements..docx
/TMC Black Logo.png
/TMC White Logo.png
/README.md
/FIRESTORE_MIGRATION.md
/BUILD_DELIVERY.md
*.docx
*.pdf
*.pptx
*.xlsx

# CI / Cloud Build / Docker plumbing
.gcloudignore
cloudbuild.yaml
deploy.ps1
Dockerfile
.dockerignore

# Logs
logs/
*.log
